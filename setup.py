from distutils.core import setup
setup(
  name = 'terminal',
  packages = ['terminal'], # this must be the same as the name above
  version = '0.1',
  description = 'Terminal.com API wrapper for python',
  author = 'Enrique Conci',
  author_email = 'qmax.mail@gmail.com',
  url = 'https://github.com/terminalcloud/terminal-pytho', # use the URL to the github repo
  download_url = 'https://github.com/peterldowns/mypackage/tarball/0.1', # I'll explain this in a second
  keywords = ['testing', 'logging', 'example'], # arbitrary keywords
  classifiers = [],
)